<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Slahly_FINAL">
        <activity
            android:name=".empty"
            android:exported="false" />
        <activity
            android:name=".reservation"
            android:exported="false" />
        <activity
            android:name=".ui.home.LogOut"
            android:exported="false" />
        <activity
            android:name=".ui.home.ContactUs"
            android:exported="false" />
        <activity
            android:name=".ui.home.Help"
            android:exported="false" />
        <activity
            android:name=".ui.home.AboutUs"
            android:exported="false" />
        <activity
            android:name=".ui.home.ToggledList"
            android:exported="false"
            android:label="@string/title_activity_toggled_list"
            android:theme="@style/Theme.Slahly_FINAL.NoActionBar" />
        <activity
            android:name=".Mechanics"
            android:exported="true" />
        <activity
            android:name=".Engravers"
            android:exported="false" />
        <activity
            android:name=".Carpenters"
            android:exported="true" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="12451000"
            tools:replace="android:value" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/map_key" />

        <activity
            android:name=".MapsActivity"
            android:exported="false"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".ui.home.Home"
            android:exported="true" />
        <activity
            android:name=".SignUp"
            android:exported="true" />
        <activity
            android:name=".SignIn"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>